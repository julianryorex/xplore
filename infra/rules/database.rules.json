{
  "rules": {
    ".read": false,
    ".write": false,
    "locations": {
      "$tripId": {
        ".read": "auth != null",
        "$uid": {
          ".write": "auth != null && auth.uid == $uid"
        }
      }
    }
  }
}
