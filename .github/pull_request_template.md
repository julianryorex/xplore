## Summary

<!-- What changed and why (1–3 bullets). -->

## Linked issues

<!-- Required when this PR completes tracked GitHub work. GitHub auto-closes on merge only when one of these keywords is present: Closes, Fixes, Resolves. -->

Closes #

<!-- Partial work or follow-ups: Related: #N (do not use Closes). -->

## Test plan

- [ ] `make verify` (codegen → check-format → analyze → test) passes.
- [ ] If this PR changes backend/infra behavior, include the relevant Terraform commands run (for example `terraform plan`, and `terraform apply` if deployed) plus any notable output or follow-up needed.

### Step-by-step verification workflow

<!--
Numbered, reproducible steps a reviewer can follow to confirm the feature actually
works (not just that tests pass). Cover the happy path plus the key edge cases this
PR touches. Note the device/target used (e.g. iOS simulator) and the expected result
at each step.
-->

1.
2.
3.
